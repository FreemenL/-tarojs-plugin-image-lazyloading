{
  "name": "root",
  "private": true,
  "scripts": {
    "prepublish": "git add .&& git commit -m'feat: new feature'",
    "publish": "cd ./packages/taro && npm run build && lerna publish "
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/FreemenL/tarojs-plugin-image-lazyloading.git"
  }
}